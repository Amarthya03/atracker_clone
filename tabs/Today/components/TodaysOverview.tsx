import { View } from "react-native";
import { Text } from "react-native-paper";
import React from "react";

const TodaysOverview = () => {
	return (
		<View style={{ flex: 1 }}>
			<Text variant="headlineLarge">Overview for today</Text>
		</View>
	);
};

export default TodaysOverview;
